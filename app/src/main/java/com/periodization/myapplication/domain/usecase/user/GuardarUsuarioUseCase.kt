package com.periodization.myapplication.domain.usecase.user

class GuardarUsuarioUseCase {
    fun execute(nombre: String, edad: Int) {
        println("Guardando usuario en base de datos...")
    }
}